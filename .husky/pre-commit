# Hook pre-commit (Husky v9):
# 1. lint-staged: eslint --fix + prettier --write solo sobre los archivos staged.
# 2. Typecheck de TODO el proyecto (npm run check equivale a tsc --noEmit).
# 3. Tests: la suite completa debe pasar antes de commitear.
bunx lint-staged
bun run check
bun test